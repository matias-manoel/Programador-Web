var m := matriz[5, 10]
var num := 0
var soma := 0
para i de 0 até 4 faça
	para j de 0 até 9 faça
		m[i][j] := leia_inteiro
	fim
fim
num := leia_inteiro
para i de 0 até 4 faça
	para j de 0 até 9 faça
		se j <> 9 então
			imprima "{m[i][j]} "
			se num == j + 1 então
				soma := soma + m[i][j]
			fim
		senão
			imprima m[i][j]
			se num == j + 1 então
				soma := soma + m[i][j]
			fim
		fim
	fim
	escreva
fim
escreva
escreva soma